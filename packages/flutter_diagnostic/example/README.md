# flutter_diagnostic example

This example shows how to use the `flutter_diagnostic` package in a Flutter application.

## Run the example

```bash
cd packages/flutter_diagnostic/example
flutter pub get
flutter run
```
