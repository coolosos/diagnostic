# diagnostic example

This example shows how to use the `diagnostic` package in a Dart application.

## Run the example

```bash
cd packages/diagnostic/example
dart pub get
dart run bin/main.dart
```
